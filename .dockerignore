# Ignore .git (version control files / all sources: not needed for production)
.git

# Ignore .vscode (code editor setting: not needed for production)
.vscode

# Editor (VS Code) settings file: not needed for production)
.editorconfig

# .gitignore (stops files from being stored in code repository: not required for production)
.gitignore

# Any (temporary) saved files, ending on .saved)
.saved

# tsconfig.jon file (typescript configuration file: not needed for production)
tsconfig.json

# tslint.json (linter configuration file for code editor: not needed for production)
tslint.json

# favicon (in subfolder of distr folder: at this place not needed for production
favicon.png

# Ignore e2e folder (test folder: not needed for production)
e2e

# Ignore coverage folder (????: no idea what this is, lets start by excluding it for production)
coverage

# node_modules folder (because generated by the dockerfile when building the image)
node_modules

# src folder (all source files, not deeded only the compile result s in the distr folder are needed and thgis copied by the dockerfile)
src







